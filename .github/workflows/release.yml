name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: zakodium/workflows/.github/workflows/release.yml@release-v1
    secrets:
      github-token: ${{ secrets.BOT_TOKEN }}
